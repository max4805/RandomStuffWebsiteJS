{
  "name": "max480-random-stuff",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "build": "rm -rf target && cd api && npm install && cd .. && cp -r api target",
    "start": "cd target && npm run start",
    "test": "cd api && npm run lint-nofix && npm run test"
  }
}
