{
  "name": "max480-random-stuff",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "build": "bash -c ./build.sh",
    "start": "cd target && npm run start",
    "test": "cd api && npm run lint-nofix && npm run test && cd ../front && npm run lint && npm run test:unit"
  }
}
